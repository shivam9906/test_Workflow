name: Test
on: push

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Hello Test
              run: echo "Hello"
            - name: clone repo
              uses: actions/checkout@v2
              run: echo "Goodbye"
